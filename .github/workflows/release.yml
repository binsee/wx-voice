name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build-platform:
    name: Build ${{ matrix.platform }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux-x64
            runner: ubuntu-latest
            cross: false
            exe_suffix: ''

          - platform: linux-arm64
            runner: ubuntu-latest
            cross: true
            cross_cc: aarch64-linux-gnu-gcc
            cross_ar: aarch64-linux-gnu-ar
            cross_ranlib: aarch64-linux-gnu-ranlib
            cross_pkg: gcc-aarch64-linux-gnu
            exe_suffix: ''

          - platform: darwin-x64
            runner: macos-13
            cross: false
            exe_suffix: ''

          - platform: darwin-arm64
            runner: macos-latest
            cross: false
            exe_suffix: ''

          - platform: win32-x64
            runner: windows-latest
            cross: false
            exe_suffix: '.exe'

          - platform: win32-arm64
            runner: windows-11-arm
            cross: false
            exe_suffix: '.exe'

    steps:
      - uses: actions/checkout@v4

      - name: Install cross-compiler (linux-arm64 only)
        if: matrix.cross == true
        run: |
          sudo apt-get update
          sudo apt-get install -y ${{ matrix.cross_pkg }}

      - name: Compile Silk SDK (Unix)
        if: runner.os != 'Windows'
        run: |
          if [ "${{ matrix.cross }}" = "true" ]; then
            make -C silk CC=${{ matrix.cross_cc }} AR=${{ matrix.cross_ar }} RANLIB=${{ matrix.cross_ranlib }}
          else
            make -C silk
          fi

      - name: Compile Silk SDK (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          choco install mingw -y
          export PATH="/c/ProgramData/chocolatey/lib/mingw/tools/install/mingw64/bin:$PATH"
          make -C silk CC=gcc CXX=g++ AR=ar RANLIB=ranlib EXESUFFIX=.exe

      - name: Copy binaries to platform package dir
        shell: bash
        run: |
          PKG_DIR="npm/wx-voice-silk-${{ matrix.platform }}"
          cp silk/encoder${{ matrix.exe_suffix }} $PKG_DIR/encoder${{ matrix.exe_suffix }}
          cp silk/decoder${{ matrix.exe_suffix }} $PKG_DIR/decoder${{ matrix.exe_suffix }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: Sync version
        run: node scripts/sync-version.js

      - name: Publish platform package
        run: npm publish npm/wx-voice-silk-${{ matrix.platform }}/ --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  publish-main:
    name: Publish main package
    needs: build-platform
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Sync version
        run: node scripts/sync-version.js

      - name: Build TypeScript
        run: npm run build

      - name: Publish main package
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
